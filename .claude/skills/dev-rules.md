# 開発ルール

Ars プロジェクトで Claude Code が守るべきルール。

## 開発サーバーの起動禁止

`npm run dev`、`vite`、`cargo watch` 等の開発サーバーを Claude Code から起動しないこと。

- ユーザーが別ターミナルでホットリロードサーバーを常時起動している
- ポート競合が発生し、既存サーバーを殺してしまう
- UI の動作確認はユーザーにブラウザで確認してもらう
