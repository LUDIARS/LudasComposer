# =============================================================================
# Ars Docker Compose 環境変数
#
# ⚠️ アプリケーション設定は Infisical で管理されます。
#    → secrets.toml.example を参照
#
# このファイルには Docker Compose 固有の設定のみ記載します。
#   cp .env.example .env
# =============================================================================

# 使用するイメージのタグ (default: latest)
IMAGE_TAG=latest

# --- Docker Compose ---
ARS_EDITOR_PORT=5173
REDIS_PORT=6379
