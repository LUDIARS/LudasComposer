# =============================================================================
# Ars - Docker Compose
#
# 統合Webサーバー（Editor + Resource Depot + Data Organizer）を起動します。
# Redis: セッション管理
# SurrealDB (embedded): ユーザー・プロジェクト・設定（グラフDB）
#
# 使用方法:
#   docker compose up -d        # サービス起動
#   docker compose logs -f      # ログ確認
#   docker compose down         # サービス停止
#
# ローカルビルド:
#   docker compose up -d --build
#
# 環境変数:
#   IMAGE_TAG (default: latest) - 使用するイメージのタグ
# =============================================================================

services:
  redis:
    image: redis:7-alpine
    container_name: ars-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    restart: unless-stopped

  ars:
    image: ghcr.io/ludiars/ars:${IMAGE_TAG:-latest}
    build: .
    container_name: ars
    ports:
      - "${ARS_PORT:-5173}:5173"
    environment:
      - PORT=5173
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID:-}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET:-}
      - GITHUB_REDIRECT_URI=${GITHUB_REDIRECT_URI:-http://localhost:5173/auth/github/callback}
      - SURREALDB_DATA_DIR=/app/data/surrealdb
      - REDIS_URL=redis://redis:6379
    volumes:
      - ars-data:/app/data
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  ars-data:
  redis-data:
